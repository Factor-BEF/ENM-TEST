# Processed data

This directory is reserved for compact, reproducible processed datasets generated by the ODMAP historical ENM workflow. It is intentionally tracked in Git.

Expected generated files include period-specific WorldClim BIO rasters, GHM rasters, cleaned red-fox occurrence tables, black-spruce observed rasters, source manifests, and compact modeling tables.

Raw large downloads remain excluded under `data_raw/`.
